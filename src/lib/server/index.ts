export { load as loadDocument, save as saveDocument, newDocument } from './database/qcm';
export { load as loadAnswer, save as saveAnswer } from './database/answer';
export { load as loadAccess, newAccess } from './database/access';
